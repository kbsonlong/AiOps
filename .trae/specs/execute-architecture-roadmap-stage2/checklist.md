* [ ] 向量存储/嵌入配置已纳入 Settings，VectorStoreManager 不再依赖散落硬编码

* [ ] 缓存层提供统一接口与内存 TTL 默认实现，并在关键路径启用（可开关）

* [ ] 事件总线支持发布/订阅，单处理器异常不影响其他处理器

* [ ] 中间件链可组合且已承载原有 skill middleware 的核心能力，工作流结果字段保持兼容

* [ ] /health 与 /ready 仅做应用自检，不包含任何外部组件连通性探测（有单测防回归）

