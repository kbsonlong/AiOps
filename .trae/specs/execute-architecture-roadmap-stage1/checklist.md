* [x] `.env.example` 不再包含 `APP_*`，配置入口统一为 `AIOPS_`

* [x] 启动/入口处存在配置校验，缺失关键项时给出明确错误

* [x] 项目存在统一异常体系与错误处理策略，关键路径不再裸捕获

* [x] 工作流不再因知识库 gate 引入额外 LLM 调用，负向响应可直返

* [x] 内置技能注册在进程内只发生一次，工作流与 Skill API 复用同一注册表

* [x] FastAPI 提供 `/health` 与 `/ready`，并具备依赖可达性检查（可 mock 测试）
